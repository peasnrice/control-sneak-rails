<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @gameroom.title %>
</p>

<p>
  <% @gameroom.users.each do |user| %>
    <%= user.username %>
    <%= link_to 'Create Phrase', new_gameroom_gameword_path(@gameroom, :recipient_id => user.id) %>
  <% end %>
</p>
<p>
  <strong>User:</strong>
  <%= @gameroom.host_id %>
</p>

<%= link_to 'Edit', edit_gameroom_path(@gameroom) %> |
<%= link_to 'Back', gamerooms_path %>
