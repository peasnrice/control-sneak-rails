<%= form_for(gameroom) do |f| %>
  <% if gameroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gameroom.errors.count, "error") %> prohibited this gameroom from being saved:</h2>

      <ul>
      <% gameroom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
